import React from 'react';
import { Box, Typography } from '@mui/material';

const BuybackTest: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Test de la page Rachat</Typography>
      <Typography variant="body1">Cette page fonctionne correctement.</Typography>
    </Box>
  );
};

export default BuybackTest;
